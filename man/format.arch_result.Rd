% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result.R
\name{format.arch_result}
\alias{format.arch_result}
\title{Format a check result}
\usage{
\method{format}{arch_result}(x, ...)
}
\arguments{
\item{x}{An \code{arch_result} from \code{\link[=arch_check]{arch_check()}}.}

\item{...}{Ignored.}
}
\value{
A character vector, one element per line of the summary.
}
\description{
Builds the same readable summary \code{\link[=print.arch_result]{print.arch_result()}} displays, but returns
it as a character vector instead of writing it to the console. Use it when
you want to route the summary somewhere else -- a log, a CI annotation, a
file.
}
\examples{
project <- system.file("examples", "layered", package = "aRchtest")

summary_lines <- format(arch_check(
  rule("UI must not compute statistics") |>
    modules_matching("R/ui_*.R") |>
    must_not_call(packages = "stats"),
  root = project
))

writeLines(summary_lines)
}
