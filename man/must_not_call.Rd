% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule.R
\name{must_not_call}
\alias{must_not_call}
\title{Forbid calls to packages or functions}
\usage{
must_not_call(rule, ..., packages = NULL, functions = NULL)
}
\arguments{
\item{rule}{An \code{arch_rule} that already has a selector.}

\item{...}{Not used. Present so a misspelled argument errors here rather
than surfacing later as a confusing check result.}

\item{packages}{A character vector of package names the selected modules
must not call.}

\item{functions}{A character vector of function names the selected modules
must not call. Bare names such as \code{"system"} match the function whoever
owns it; qualified names such as \code{"utils::head"} match only that owner.}
}
\value{
The rule, with the constraint attached.
}
\description{
\code{must_not_call()} states that the modules a rule selects must not call the
given packages, the given functions, or both. It is a negative constraint:
it never asserts what the modules \emph{may} call.
}
\details{
A call is attributed to an owner before the constraint is applied, so both
\code{DBI::dbGetQuery()} and a bare \code{dbGetQuery()} are caught -- a rule cannot be
evaded by dropping the \code{::} prefix. A call the analysis cannot attribute is
never reported as a violation; see \code{\link[=arch_check]{arch_check()}} for how to read the
resolution counts that tell you how much was attributed.

Base packages count as declared, so \code{functions = "system"} fires on a bare
\code{system()} call. A function the project defines itself always wins over a
package export of the same name, so a local helper never produces a
violation.
}
\examples{
rule("UI must not reach the database") |>
  modules_matching("R/ui_*.R") |>
  must_not_call(packages = c("DBI", "RPostgres"))

rule("No shelling out") |>
  modules_matching("R/*.R") |>
  must_not_call(functions = c("system", "setwd"))
}
