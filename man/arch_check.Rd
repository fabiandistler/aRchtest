% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{arch_check}
\alias{arch_check}
\title{Check architectural rules against a project}
\usage{
arch_check(rules, root = ".", on_ambiguous = c("report", "fail"))
}
\arguments{
\item{rules}{A single \code{arch_rule} or a list of them.}

\item{root}{Path to the project root to analyse. Defaults to the working
directory. All file selection and every reported path is relative to this
root, so rules read the same wherever the tests run from.}

\item{on_ambiguous}{What to do with a call whose symbol is exported by more
than one candidate package and which is not defined locally.
\code{"report"}, the default, puts such a call in the \code{ambiguous} table and
keeps it out of \code{violations}. \code{"fail"} counts it as a violation.}
}
\value{
An \code{arch_result}: a list with
\itemize{
\item \code{violations} -- a \code{data.table} with one row per finding and columns
\code{rule}, \code{file}, \code{line}, \code{column}, \code{enclosing_function}, \code{callee},
\code{owner}, \code{resolved_by}, \code{internal} and \code{source_text}. On success it is
empty but has the same columns and types, never \code{NULL}.
\item \code{ambiguous} -- near misses that could not be attributed confidently.
\item \code{rules} -- one row per rule, with the number of files it selected,
whether its selector matched nothing, and how many violations it found.
\item \code{selected_files} -- per rule, the files its selector matched.
\item \code{counts} -- call sites seen, resolved, ambiguous and unresolved.
\item \code{files_parsed}, \code{skipped_files} -- parse coverage.
\item \code{declared_dependencies}, \code{unavailable_dependencies} -- resolution inputs.
\item \code{root}, \code{is_package}, \code{on_ambiguous} -- how the check was scoped.
}
}
\description{
\code{arch_check()} performs the analysis a rule describes and returns the
findings as data. It discovers the project's source files, parses them,
attributes every call it finds to an owner, and applies each rule's
constraints to the calls in the files that rule selects.
}
\details{
The project under analysis is never sourced, loaded or evaluated -- only its
parse data is read. Checks are therefore deterministic, safe to run in
continuous integration, and work on a project that does not currently run.

The project is discovered, parsed and resolved \strong{once} per call regardless
of how many rules are supplied, so expressing more architecture does not
cost proportionally more time.
}
\section{Resolving unqualified calls}{
Attributing a bare \code{dbGetQuery()} to \code{DBI} means enumerating what \code{DBI}
exports, which requires the package to be \strong{installed} in the library path
of the R session running the check. A declared dependency that cannot be
loaded degrades resolution rather than erroring: its calls fall into the
unresolved bucket and the package is listed in \code{unavailable_dependencies}.

Read \code{counts} before trusting a clean result. A check that resolved little
of what it saw proves little, and a degraded pass looks exactly like a clean
project unless you look.
}

\examples{
project <- system.file("examples", "layered", package = "aRchtest")

result <- rule("UI must not reach the database") |>
  modules_matching("R/ui_*.R") |>
  must_not_call(packages = "stats") |>
  arch_check(root = project)

result
result$violations
}
