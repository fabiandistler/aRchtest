% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule.R
\name{modules_matching}
\alias{modules_matching}
\alias{modules_matching_regex}
\title{Select the modules a rule applies to}
\usage{
modules_matching(rule, pattern, ...)

modules_matching_regex(rule, pattern, ...)
}
\arguments{
\item{rule}{An \code{arch_rule} from \code{\link[=rule]{rule()}}.}

\item{pattern}{A single glob pattern (\code{modules_matching()}) or regular
expression (\code{modules_matching_regex()}), matched against file paths
relative to the project root.}

\item{...}{Not used. Present so a misspelled argument errors here rather
than surfacing later as a confusing check result.}
}
\value{
The rule, with the selector attached.
}
\description{
\code{modules_matching()} narrows a rule to the files matched by a glob pattern.
\code{modules_matching_regex()} does the same with a regular expression, for when
file naming is too irregular for a glob. Both match against paths relative
to the project root, so a rule reads the same wherever the tests are run
from.
}
\details{
In a glob, \code{*} matches any run of characters within one path segment, \verb{**}
matches across segments, and \verb{?} matches a single character within a
segment. Everything else is literal. Reach for the regex variant only when a
glob cannot express the selection -- a glob is easier for the next reader.

Calling a selector more than once on the same rule widens the selection: a
file is selected if it matches any of the rule's patterns.
}
\examples{
rule("layering") |> modules_matching("R/ui_*.R")

rule("layering") |> modules_matching_regex("^R/(ui|view)_.*\\\\.R$")
}
