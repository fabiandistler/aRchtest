% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result.R
\name{arch_violations}
\alias{arch_violations}
\title{The violations a check found}
\usage{
arch_violations(result)
}
\arguments{
\item{result}{An \code{arch_result} from \code{\link[=arch_check]{arch_check()}}.}
}
\value{
A \code{data.table} of violations.
}
\description{
A convenience accessor for the violations table of an \code{\link[=arch_check]{arch_check()}} result.
The table has one row per finding and is a \code{data.table}, so it can be
filtered, grouped and summarised with tools you already use. When a check
passes it is empty but keeps the same columns and types, so code consuming
it needs no special case for success.
}
\examples{
project <- system.file("examples", "layered", package = "aRchtest")

rule("UI must not reach the database") |>
  modules_matching("R/ui_*.R") |>
  must_not_call(packages = "stats") |>
  arch_check(root = project) |>
  arch_violations()
}
