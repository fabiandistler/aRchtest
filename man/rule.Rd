% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule.R
\name{rule}
\alias{rule}
\title{Start an architectural rule}
\usage{
rule(name, why = NULL)
}
\arguments{
\item{name}{A single string naming the rule. The name appears in every
violation the rule produces and in the failure message, so it should read
as the architectural agreement being enforced.}

\item{why}{An optional single string explaining \emph{why} the boundary exists.
It is shown in the failure message, so a future reader learns the
rationale and not only that a line was crossed.}
}
\value{
An \code{arch_rule} object.
}
\description{
\code{rule()} opens a rule. A rule is inert data: building one performs no
analysis, so rules are cheap to construct, can be stored in a list, and can
be shared across tests. Add a selector with \code{\link[=modules_matching]{modules_matching()}} or
\code{\link[=modules_matching_regex]{modules_matching_regex()}}, add a constraint with \code{\link[=must_not_call]{must_not_call()}} or
\code{\link[=must_not_depend_on]{must_not_depend_on()}}, and check it with \code{\link[=arch_check]{arch_check()}} or
\code{\link[=arch_expect]{arch_expect()}}.
}
\examples{
rule("UI must not reach the database", why = "Presentation stays thin")
}
