% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule.R
\name{must_not_depend_on}
\alias{must_not_depend_on}
\title{Forbid a dependency on other modules of the same project}
\usage{
must_not_depend_on(rule, ..., modules, regex = FALSE)
}
\arguments{
\item{rule}{An \code{arch_rule} that already has a selector.}

\item{...}{Not used. Present so a misspelled argument errors here rather
than surfacing later as a confusing check result.}

\item{modules}{A character vector of patterns naming the forbidden modules.}

\item{regex}{Whether \code{modules} are regular expressions. The default, \code{FALSE},
reads them as globs, matching \code{\link[=modules_matching]{modules_matching()}}.}
}
\value{
The rule, with the constraint attached.
}
\description{
\code{must_not_depend_on()} enforces a boundary inside your own code rather than
against an external package. The modules a rule selects must not call
functions defined in the modules named here, so a \verb{domain/} file reaching
into an \verb{infra/} helper fails with the same located finding an external
violation would produce.
}
\details{
Forbidden modules are named with the same selector vocabulary the rule's own
selector uses, resolved relative to the project root. No \code{::} is required:
the constraint works on ordinary unqualified calls between project files. A
module calling a function it defines itself is never a violation.
}
\examples{
rule("domain must not know about infrastructure") |>
  modules_matching("R/domain_*.R") |>
  must_not_depend_on(modules = "R/infra_*.R")
}
